

前端请求接口：
网关 -> 转发到具体服务

UEL java EL 工具类${}


可以实现jpa json 查看com.goudong.oauth2.po.BaseMenuPO
<dependency>
    <groupId>com.vladmihalcea</groupId>
    <artifactId>hibernate-types-52</artifactId>
    <version>${hibernate-types-52.version}</version>
</dependency>


## 一些小sql汇总
用于保存sql脚本，太多文件不太好，强迫症


## jpa

这种方式会生成一个 hibernate_sequence 表存储序列。seata使用会报错
```java
@Id
@GeneratedValue(strategy= GenerationType.AUTO)
private Long id;
```

## Feign
feign 在调用时如果参数丢失，需要加上参数注解
```java
@DeleteMapping("/warehouse/del-warehouse")
Result<Object> save(@RequestParam("product") String product);
```

## TODO
+ 脱敏工具开发
+ 签名防止参数修改
